import { TorreDetailsPipe } from './torre-details.pipe';

describe('TorreDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new TorreDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
